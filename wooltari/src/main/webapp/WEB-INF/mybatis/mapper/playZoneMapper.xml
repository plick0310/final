<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="playZone">
	<select id="pointlist" parameterType="map" resultType="com.wooltari.playZone.PlayZone">
	 select userid, username, point from member where userid=#{userId}
	 </select>
</mapper>