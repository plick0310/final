<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test">
	<sql id="where-list">
		<if test="searchKey=='examInfoName'">
			examInfoName=#{searchValue}
		</if>
		<if test="searchKey=='questionSubject'">
			questionSubject=#{searchValue}
		</if>
	</sql>
	
	<!-- 모의 테스트 리스트 -->
<!-- 	<select id="listTest" parameterType="map" resultType="com.wooltari.test.Test">
	
	</select> -->
	
	<!--  -->
	
</mapper>